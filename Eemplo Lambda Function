import boto3

def lambda_handler(event, context):
    s3 = boto3.client('s3')
    
    # Nome do bucket e arquivo de exemplo
    bucket_name = 'meu-bucket-dio-exemplo'
    file_name = 'exemploteste.txt'
    file_content = 'Conteúdo de teste enviado via Lambda'
    
    # Enviando arquivo para o bucket S3
    s3.put_object(Bucket=dio_luna, Key=file_name, Body=file_content)
    
    return {
        'statusCode': 200,
        'body': f'Arquivo {file_name} criado no bucket {dio_luna} com sucesso!'
    }
